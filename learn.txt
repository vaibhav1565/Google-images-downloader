from selenium import webdriver
from selenium.webdriver.common.keys import Keys
from selenium.webdriver.common.by import By
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.support import expected_conditions as EC

driver = webdriver.Firefox()
driver.get("http://www.python.org")
element = WebDriverWait(driver, 10).until(
        EC.presence_of_element_located((By.ID, "myDynamicElement"))
)
To move backward and forward in your browser’s history:
driver.forward()
driver.back()

TAKE SCREENSHOT OF CURRENT WINDOW
driver.save_screenshot('screenshot.png')


assert "Python" in driver.title

elem = driver.find_element(By.NAME, "q")
To find multiple elements (these methods will return a list):

    find_elements


elem.clear()
elem.send_keys("pycon")
elem.send_keys(Keys.RETURN)
assert "No results found." not in driver.page_source

driver.close() #JUST ONE TAB
driver.quit()  #ENTIRE BROWSER










SELECT TAG
element = driver.find_element(By.XPATH, "//select[@name='name']")
all_options = element.find_elements(By.TAG_NAME, "option")
for option in all_options:
    print("Value is: %s" % option.get_attribute("value"))
    option.click()
MORE EFFICIENT WAY GIVEN IN DOCUMENTATION
3.3. Drag and drop¶
3.4. Moving between windows and frames¶
3.5. Popup dialogs¶
3.7. Cookies¶








Here is an example to scroll to the bottom of a page:
driver.execute_script("window.scrollTo(0, document.body.scrollHeight);")
The scrollHeight is a common property for all elements. The document.body.scrollHeight will give the height of the entire body of the page.
window.scrollTo(x, y) method
LIST OF EXPECTED CONDITIONS
    title_is

    title_contains

    presence_of_element_located

    visibility_of_element_located

    visibility_of

    presence_of_all_elements_located

    text_to_be_present_in_element

    text_to_be_present_in_element_value

    frame_to_be_available_and_switch_to_it

    invisibility_of_element_located

    element_to_be_clickable

    staleness_of

    element_to_be_selected

    element_located_to_be_selected

    element_selection_state_to_be

    element_located_selection_state_to_be

    alert_is_present


driver.implicitly_wait(0.5)